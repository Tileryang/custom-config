const assert = require('assert');
const customConfig = require('../index.js');

describe('customConfig', () => {
  it('has a test', () => {
    assert(false, 'customConfig should have a test');
  });
});
